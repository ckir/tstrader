node_modules/
dist/
.turbo/
coverage/
*.log
.DS_Store

# dotenvx: the encrypted .env is committed; private keys never are (wired in a later increment)
.env.keys

# Playwright (later increment)
playwright-report/
test-results/
